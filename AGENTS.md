# 掌舵六窗口共同规则

先读取 `coordination/接口契约说明.md`、`coordination/assignments.json` 和本窗口在 `../codex节点/` 的任务包。未明确窗口编号时，不开始修改代码，先向用户询问自己承担哪个窗口。

## 本轮 AI 接入专项分工

用户已明确授权本轮 A/B/C/D 并行：A 是当前窗口，也是原窗口一的总控；B/C/D 是窗口一原有范围内的专项分工，不是原窗口二/三/四。收到 B/C/D 任务提示词即已明确身份，不再询问窗口编号。

本轮先读 `coordination/AI接入并行/README.md` 和对应角色的 `任务提示词.md`。这份专项文件的精确写入白名单是对下面目录边界的补充授权；仅在列出的文件上适用，不改变原窗口二至六的业务模块归属。A 在并行期间也不修改已交给 B/C/D 的文件。依赖安装、公共契约、共享服务和最终集成仍由 A 负责。

### 当前阶段：最终检查、缺陷修复与交付收口

用户新开启最终检查阶段，重新允许必要的代码修复、单元/集成测试、类型检查、构建、真实浏览器验证和离线合成训练。下方“暂停测试”为历史阶段，不再限制本轮。以当前源码和本轮证据为准，不复用旧数量声称新修改通过，不新增无关功能或派生代理/窗口。

A 负责公共契约、platform/server、App/路由、共享身份/api-client、共享配置/依赖和集成工具；B 独占训练文件，C 独占两张 AI 面板及其客户端/专属测试，D 重新独占根 README 和接入文档。A 不再编辑 D 文档。原 features 有闭环阻塞时，先核对负责人冻结并登记文件级接管范围，才做必要修复，不并行双写。

允许 A 登记并管理专用临时 loopback fixture 服务，隔离数据库、合成资料和可控外部适配器，不加载私人 .env；将地址、模式、停止方式公布给 C/D。历史 4312/4313 live 服务不擅自重启、不用于写入测试。真实 CNB 写入、付费模型、下载大模型、部署、公开、远端删除或读取私人凭据仍未默认授权。

A 按功能到页面/请求/HTTP/Services/存储或 worker/读回/证据建立矩阵。各角色冻结后运行最终全库门；B 冻结 worker 后再运行隔离 HTTP 到真实 Python 合成联合，避免重复 CPU 任务。最终报告为 `coordination/AI接入并行/A/最终收尾报告.md`，分别判断源码可冻结、合成演示可用、真实环境已验证；所有检查/训练进程结束，仅登记的预览服务可保留。

### 历史阶段：功能与 README 优先，暂停测试

此前用户要求：架构的功能在现有作品中呈现，先不进行测试，完成功能和 README 即可。以下仅保留当时的安排与证据边界，不覆盖上方新开启的最终检查阶段：当时不执行单测、集成测试、浏览器验收、类型检查、训练 smoke 或真实 LoRA，不把静态核对写成运行验证通过。

D 已在交付报告中冻结文档；A 按用户本次 README 要求接续整理根 `README.md` 与 `coordination/对话沉淀与训练接入.md`，保留 D 的历史报告。B/C 的源码边界和原窗口二至六的所有权不变。不重启其他维护窗口持有的 live 服务，不执行真实 CNB 或模型操作。既有测试文件与证据保留，后续是否测试由用户另行决定。

## 范围与目的

这是腾讯CNB个人知识图谱作品。Issue保留授权对话现场，Git保留人的正式知识和关系，CNB知识库参与检索，AI只提出候选。图谱必须参与检索而非装饰；应用结果可触发修订，人的主权贯穿全流程。

## 并行写入边界

1. 唯一代码根目录是本目录“作品”。不得在节点说明目录或桌面另起一套项目。
2. 窗口二至六只修改 `src/features/<自己的feature>/`，以及自己任务包里的接口申请和交付报告。严禁修改其他模块。
3. 窗口一维护共享契约、依赖、平台、组合入口、全局样式和集成测试；不能擅自重写其他窗口正在开发的模块。
4. 所有窗口看到的未提交变更可能属于别人，禁止reset、checkout还原、clean、全目录格式化或全局替换。
5. 只有窗口一安装依赖和修改锁文件、启动共享dev server、运行全库自动修复。其他窗口运行自己目录测试，不争同一端口。
6. 不自动git commit、push或操作分支。共享工作树不靠切换分支隔离；用目录所有权和契约协调。需要独立worktree时先单独协调集成策略。
7. 不继续派生子代理或新增窗口，除非用户另行授权；这份文件只定义用户已分配的六窗口协作。

## 代码接口

业务模块保持 `client.tsx: Page`、`server.ts: registerRoutes` 导出。跨模块只能通过 `src/contracts`、Services和约定API交互，不import兄弟模块内部文件。共享契约只由窗口一修改，任何变更先更新版本与影响清单。

## 逐节点实现

每次只取自己任务列表中的一个原子任务：明确输入/输出 → 写失败测试 → 小范围实现 → 运行测试与类型检查 → 记录结果 → 再继续下一任务。小节点通过不代表整个模块通过，模块通过不代表系统通过。

新增UI遵守原生语义控件、键盘、读屏、非颜色状态、320px窄屏和长文本。自己的CSS必须在 `.capture`、`.handoff` 等模块根元素内，不改全局body、nav、button等规则。

## 安全与真实性

不得读取私人浏览历史或聊天库。真实CNB账号调用、写库、付费模型、部署、公开和删除必须获得用户相应授权。没有授权先使用明确fixture完成端口和业务单测。

Token只在服务端；导入内容是不可信数据。无批准不发送或持久化；AI输出不过schema和引用检查不入正式知识。Git/Issue物理删除、模型清单、成本和限流不允许凭印象假定。

不要把fixture标live，不要让未实现端点返回虚假业务成功，不要声称用户已经掌握。所有对外状态区分designed、implemented_fixture、integrated、verified_live。

## 完成时

更新自己任务包的交付报告：改动文件、通过的测试命令、未完成项、接口申请、集成风险及是否真实调用CNB。窗口二至六完成后由窗口一按全局集成门验收。
