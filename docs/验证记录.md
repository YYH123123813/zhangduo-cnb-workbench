# 验证记录

本次按用户要求交付项目简述 README 与工程源码快照。专项最终检查仍由各维护窗口分别收口，因此这里不宣称所有专项已经冻结或完整真实环境已通过；历史数量不自动算作本次通过。

## 验证分层

| 层次 | 允许据此说明什么 | 不能据此说明什么 |
| --- | --- | --- |
| TypeScript、目录边界与构建 | 对应快照能够通过静态门和产出前端构建 | 真实 CNB、浏览器或生产部署已通过 |
| 合成单元与 HTTP/Services 集成 | 所覆盖的业务规则与受控传输链路符合断言 | 外部平台支持相同能力、模型具备真实质量 |
| 真实浏览器的合成工作流 | 指定视口和操作在该浏览器中实际完成 | 全平台、完整读屏、所有功能全部通过 |
| Python 合成训练 | 指定源码真实更新参数、验证权重与保存重载 | 预训练模型质量提高、个人知识被正确记住 |
| 历史受限 CNB 读回 | 当时明确授权范围的读回事实 | 本次完整线上闭环或单条 Issue 私密属性已通过 |

## 已知边界

- 私人 `.env`、运行数据库、真实账号标识和 live 截图不随源码交付。
- 历史真实 CNB 检查发现单条 Issue 的私密标记未按请求读回；正式写入校验没有为此放宽。完整真实写入闭环不得宣称通过。
- 本轮未授权公开部署、变更真实知识仓库可见性、调用付费模型或下载大模型。
- 没有真实用户效果实验、中文提取准确率或知识掌握提升的证据。
- 停用与删除不是同一动作；逻辑阻断不保证备份、磁盘历史、外部模型或 SQLite/WAL 的物理擦除。
- 比赛报名、团队与指导教师资料、演示视频、答辩材料、CNB 评审访问尚需参赛者按实际提交要求确认。

## 发布核验

本次发布使用独立 Git 仓库的暂存快照，在临时目录导出源码，不带私人配置、数据库、训练环境或作者桌面的节点文件夹，再执行安装与检查。发布清单另由 `tools/check-release.mjs` 读取 Git 索引中的实际 blob 核验，检查私密路径、密钥模式、符号链接、文件体积和本地 Markdown 链接；只对隐私回归测试中精确匹配的固定假密钥做豁免，不跳过测试目录的其他密钥。

环境：Node.js `24.11.0`，pnpm `10.32.1`。首次隔离安装使用 `pnpm install --offline --frozen-lockfile`，退出 0，复用本机已有依赖缓存，未下载模型。离线安装成功不代表新电脑首次安装无需联网。

本次隔离测试时间为 **2026-09-16 06:34:56 至 06:36:12（Asia/Shanghai）**，对应准确源码指纹见 [机器可读记录](verification.json)。发布前逐文件核对了 549 个实现、测试、训练与运行工具文件，均与隔离目录受测副本一致；发布审计工具另有独立测试。

| 检查 | 实际结果 |
| --- | --- |
| 锁文件安装 | `pnpm install --offline --frozen-lockfile`，退出 0；使用已有缓存 |
| 类型检查 | `pnpm typecheck`，退出 0 |
| 独立目录边界 | `pnpm check:boundaries`，退出 0；75 个唯一任务 ID、82 条唯一归属路由，无跨 feature 导入；不依赖项目外任务文件夹 |
| 合成全库测试 | `pnpm exec vitest run --maxWorkers=2`，219 个文件，1853/1853 通过，0 失败、0 跳过、0 待办；退出 0 |
| 前端构建 | `pnpm build`，退出 0；1816 个模块；保留 JS chunk 大于 500 kB 的优化提示，没有调整阈值隐藏提示 |
| 发布审计单测 | `node --test tools/check-release.test.mjs`，11/11 通过 |
| 发布文件清单 | `node tools/check-release.mjs`，通过；无私密路径、未豁免的密钥模式或本地 Markdown 断链 |

沙箱首次运行 Vitest 因 `localhost` 解析限制未能启动；获得运行权限后，在同一隔离副本执行上述完整测试并通过，没有降低断言或删除失败用例。`git diff --cached --check` 另报告原有治理页面一处行尾空格，未为此次文档发布改动该业务文件；它不是上述功能测试失败。

本次没有执行新的浏览器交互、Python 梯度训练或真实 CNB 业务操作，不把其他窗口的结果算作本次执行。GitHub Actions 另配置 Node.js 22/24 的源码检查，远端运行结论须以对应提交的实际状态为准；CI 不执行部署、真实 CNB 写入或付费模型调用。不把“已推送 GitHub”当作“比赛已报名或所有外部能力已完成”。
